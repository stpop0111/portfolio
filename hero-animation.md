# Hero アニメーション設計書

ポートフォリオサイトの最初の画面、「ローディング → タイトル表示」までのアニメーション設計。
PDF `ローディング⇨タイトル表示.pdf` をベースに、文字でまとめ直したもの。

**ステータス**: ラフ段階。未確定項目あり（末尾にリスト）。

---

## 全体の流れ（5ステージ + ナビ遷移）

```
[Stage 1] ローディング画面（3Dモデル読み込み中）
   ↓
[Stage 2] Loading → Portfolio に文字変化
   ↓
[Stage 3] 黒幕が上にスライド、色が反転
   ↓
[Stage 4] PC モデル登場 + 名前タイポアニメ
   ↓
[Stage 5] キーキャップ + 吹き出しが下から出現
   ↓
[Stage 6] キーキャップ クリック → ナビ遷移（白幕が下からスライドイン）
```

---

## Stage 1: ローディング画面

| 項目 | 内容 |
|---|---|
| 背景色 | 黒（ink） |
| 文字色 | 肌色（cream） |
| 表示テキスト | `Loading...` |
| 動き | 文字が点滅 |
| **何を待つか** | **3Dモデル（.glb等）の読み込み完了**（決定 ✓） |
| **リロード表示** | **15秒経過後にリロードボタン表示**（決定 ✓） |

**実装メモ**
- 点滅は CSS の `@keyframes` で OK
- 3Dモデルの onLoad コールバックで Stage 2 へ遷移
- 15秒経過でリロードボタン表示（決定 ✓）
- 補足: UX的には5〜10秒推奨だが、世界観優先で15秒採用。気になれば「10秒で待機メッセージ→15秒でリロード」の2段階も検討余地あり

---

## Stage 2: Loading → Portfolio に文字変化

| 項目 | 内容 |
|---|---|
| 変化前 | `Loading...` |
| 変化後 | `Port/olio`（"f" の位置がスラッシュ） |
| **変化方法** | **ブラー（ぼやける → はっきりした Port/olio に）**（決定 ✓） |
| 背景 | まだ黒 |
| 文字色 | まだ肌色 |

**実装メモ**
- ブラー方式で確定（決定 ✓）
- 実装: `filter: blur(20px)` から `filter: blur(0)` へ GSAP で補間
- 同時にテキスト内容を `Loading...` → `Port/olio` に差し替え（ブラーがかかってる間に切り替えれば違和感なし）

---

## Stage 3: 画面遷移（黒幕 + 色反転）

| 項目 | 内容 |
|---|---|
| **黒幕の動き** | **パターン1：黒い幕が上にスライドして消える**（決定 ✓） |
| 時間 | 0.6s |
| イージング | ease.inOut |
| 文字色の変化 | 肌色 → 黒（cream → ink） |
| 同期 | 幕が動くのと文字色変化は同時 |

**実装メモ**
- 黒幕は `position: fixed; inset: 0; background: ink;` でフル画面のオーバーレイ
- GSAP で `y: '-100%'` にアニメ → 上にスライドアウト
- 文字色変化は同じ Timeline の同タイミングに揃える

---

## Stage 4: タイトル表示（PC + 名前）

タイトルが完成形に組まれていく。順序は以下：

```
①  PC の3Dモデルがゆっくり浮かび上がる
② Port/olio タイトルが少し上に上がる
③ 骨組みの線（赤い枠線）が矢印の方向に広がる
④ 名前（seita izaki）がタイポアニメーションで表示
```

| 項目 | 内容 |
|---|---|
| ① PC モデル | 3D 表示、フェードイン or 浮き上がる動き |
| ② タイトル位置 | 中央 → 少し上に移動 |
| ③ 骨組み線 | 矢印方向に伸びる（SVG のストロークアニメ） |
| ④ 名前 | seita / izaki、ScrambleText 系のアニメ |

**画面構成（完成形）**
- 左上: `seita`
- 中央: `Port/olio`（タイトル）
- 右下: `izaki`
- 中央下: PC の3Dモデル
- 全体に赤い枠線（骨組み）

**実装メモ**
- 順番に動かしたいので GSAP Timeline で `tl.from()` を連続で書く
- ① の PC モデル登場と ② のタイトル移動は同時 or 微妙にズラす（要決定）

---

## Stage 5: キーキャップ + 吹き出し

| 項目 | 内容 |
|---|---|
| トリガー | PC が完全に表示されたら |
| キーキャップ | 下からスライドイン |
| 吹き出し | キーキャップの後、3Dで表示 |

**キーキャップの内容**（決定 ✓）
- 4個出す
  1. `ABOUT ME` → About セクション
  2. `WORKS` → Works セクション
  3. `DESIGN` → Designs セクション
  4. `ORIGINAL WORKS` → Original Work セクション
- Contact は別扱い（ナビゲーション or フッターで対応予定）

**実装メモ**
- キーキャップ = 3Dで作る or 画像で代用、どっちにするか要決定
- 吹き出しは Three.js 内 or HTML レイヤーで重ねる

---

## Stage 6: ナビ遷移（キーキャップ押下時）

| 項目 | 内容 |
|---|---|
| **操作モデル** | **クリック → 白い画面が下からスライドイン → ページ遷移**（決定 ✓） |
| 動きの対比 | Stage 3 は黒幕が「上に」抜ける。Stage 6 は白幕が「下から」入る |

**実装メモ**
- 遷移先は Next.js の App Router で別ページ or 同ページ内の別セクションどちらでも対応可
- 白幕も Stage 3 と同様、fixed なオーバーレイで GSAP アニメ
- ページ遷移完了後、白幕は次の画面で上にスライドアウトして消える（と自然）

---

## 使用する素材

### 3Dモデル

| モデル | フォーマット | パス | 状態 |
|---|---|---|---|
| PC | ? (.glb / .gltf?) | `public/models/?` | 作成済み |
| キーキャップ | ? | `public/models/?` | 作成済み |
| 吹き出し | ? | `public/models/?` | 作成済み？ |

→ **未確定 H**（ファイル名・配置パス・フォーマット）

### 色

| トークン名 | 用途 | HEX |
|---|---|---|
| `cream` | 肌色／ベース背景 | TBD |
| `ink` | 黒／文字色・初期背景 | TBD |
| `accent` | 赤の骨組み線・差し色 | TBD |
| `white-curtain` | Stage 6 の白幕（cream と別？） | TBD |

→ **未確定 I**（具体的な HEX 値）

### フォント

- タイトル `Port/olio` のフォント: 等幅っぽい、丸めのサンセリフ → **未確定 J**
- 名前 `seita / izaki` のフォント: 同上 → **未確定 J**
- 本文用のフォント: タスクリストには Zen Old Mincho 候補

---

## モバイル対応の方針

**スマホでも同じ演出を頑張る**（決定 ✓）

**注意点・追加で考えること**
- 3Dモデル + アニメをスマホで動かすと **FPS が落ちる / 電池消費が増える** 可能性
- ローディングが長くなる可能性 → Stage 1 の演出時間で吸収できる
- 開発中はスマホ実機で FPS チェック必須
- `prefers-reduced-motion` への対応は別途検討（今は対応しない）

---

## 技術的な実装メモ

### 使うライブラリ

- **GSAP**: アニメーションのタイムライン管理
- **@gsap/react**: React で GSAP を安全に使うためのフック
- **Three.js**: 3D 描画エンジン
- **@react-three/fiber**: Three.js を React 風に書ける
- **@react-three/drei**: Three.js のよくあるパターンが揃ったヘルパー集

すべてインストール済み（タスク #14 完了時点）。

### Timeline のイメージ（疑似コード）

```ts
const tl = gsap.timeline()

// Stage 1: ローディング（3Dモデル読み込み中 / 別レイヤーで点滅 CSS）
// → onLoad で Stage 2 へ移行

// Stage 2: 文字変化
tl.to('.loading-text', { text: 'Port/olio', duration: 0.8 })

// Stage 3: 黒幕 + 色反転
tl.to('.curtain-black', { y: '-100%', duration: 0.6, ease: 'power2.inOut' })
  .to('.title-text', { color: 'var(--ink)', duration: 0.6 }, '<') // 同時

// Stage 4: タイトル
tl.from('.pc-model', { opacity: 0, y: 30, duration: 0.8 })
  .from('.title-text', { y: 20, duration: 0.6 }, '<0.2')
  .from('.frame-line', { drawSVG: 0, duration: 0.6 })
  .from('.name', { /* ScrambleText */ })

// Stage 5: キーキャップ
tl.from('.keycap', { y: 40, opacity: 0, duration: 0.5, stagger: 0.1 })
  .from('.balloon', { scale: 0, duration: 0.4 })

// Stage 6: クリックハンドラ（Timeline 外、onClick で発火）
const navigateTl = gsap.timeline({ paused: true })
navigateTl.fromTo('.curtain-white',
  { y: '100%' },
  { y: '0%', duration: 0.6, ease: 'power2.inOut' }
)
// アニメ完了後 router.push() でページ遷移
```

**GSAP 有料プラグインは使わない**（決定 ✓）
- `drawSVG` の代わりに `stroke-dashoffset` を GSAP で直接アニメ
- `ScrambleText` の代わりに、文字を1文字ずつランダム置換するロジックを自前実装
- ※ Stage 2 はブラー方式で確定済みなので ScrambleText 自体不要
- ※ 名前（seita / izaki）の表示も ScrambleText 風にしたい場合のみ自前実装

---

## 未確定事項リスト

実装に入る前に決めたいもの順で並べる。

| ID | 項目 | 影響 | 優先度 | 状態 |
|---|---|---|---|---|
| ~~A~~ | ローディングは何を待つか | Stage 1 | 高 | ✓ 3Dモデル読み込みを待つ |
| ~~B~~ | 「リロード」ボタンが出る秒数 | Stage 1 細部 | 中 | ✓ 15秒経過後 |
| ~~C~~ | 黒幕の動き | Stage 3 | 高 | ✓ パターン1（スライド） |
| ~~D~~ | モバイル対応 | 全体 | 高 | ✓ 同じ演出を頑張る |
| ~~E~~ | キーキャップ操作モデル | Stage 6 | 高 | ✓ 白幕スライド→遷移 |
| ~~F~~ | Stage 2 の文字変化 | Stage 2 表現 | 中 | ✓ ブラー方式 |
| ~~G~~ | キーキャップの個数と各キーの中身 | Stage 5 素材 | 中 | ✓ 4個（About/Works/Design/Original Works） |
| **H** | 3D モデルのファイル名・配置パス・フォーマット | 実装の入口 | 高 | 🕒 格納後に共有予定 |
| **I** | cream / ink / accent / 白幕 の具体的 HEX 値 | Tailwind 設定 | 高 | 🕒 実装しながら決定 |
| **J** | タイトル・名前のフォント（具体名） | Tailwind 設定 | 高 | 🕒 実装しながら決定 |
| ~~K~~ | GSAP 有料プラグイン（DrawSVG / ScrambleText）を使うか | 実装の組み方 | 低 | ✓ 使わない（自前実装） |

---

---

## 実装中に決めるもの（保留メモ）

以下は実装しながら触感で決めることになった。プレースホルダーで開始して、動かしながら調整する方針。

### 色（I）
- 実装開始時は仮の HEX 値で進める（PDF から目視で推測した値、または Tailwind デフォルトの近い色）
- Hero が動くようになったら、Figma で色を当てたり、ブラウザで `inspect` で触りながら調整
- 確定したら `tailwind.config.ts` のトークン定義を更新

### フォント（J）
- 実装開始時は仮のフォントで進める（Geist Mono が Next.js デフォルトで入ってる）
- 候補を 2〜3 個切り替えながら、Hero のタイトル `Port/olio` に当てて比較
- 確定したら `next/font` での読み込み設定を更新

### 3D モデル（H）
- ファイル格納後に共有してもらう
- それまでは仮のプレースホルダー（簡単な箱とか）でレイアウトだけ進める
- 格納先の慣例: `public/models/*.glb`

---

## 関連ファイル

- `ローディング⇨タイトル表示.pdf`（uploads にある元 PDF）
- `portfolio-tasks.md`（全体タスクリスト）
- `AGENTS.md`（進め方）

